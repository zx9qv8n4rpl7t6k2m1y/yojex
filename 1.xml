<?xml version="1.0" encoding="UTF-8" ?>
<Module>
<ModulePrefs title="classroom" />
<Content type="html"><![CDATA[


<!DOCTYPE html>
<html lang="en-us">
<head>
<meta charset="utf-8"/>
<meta http-equiv="Content-Type" content="text/html; charset=utf-8"/>
<meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no"/>
<style>
  *,*:before,*:after{box-sizing:border-box;}
  html,body{margin:0;padding:0;height:100%;overflow:hidden;background:#111;
    -webkit-touch-callout:none;-webkit-user-select:none;-moz-user-select:none;
    -ms-user-select:none;user-select:none;-webkit-tap-highlight-color:rgba(0,0,0,0);}
  canvas:focus{outline:none;}
  body{display:flex;align-items:center;justify-content:center;}
  #unity-container{
    position:relative;
    width:min(100vw,177.78vh);
    height:min(56.25vw,100vh);
  }
  #unity-canvas{width:100%;height:100%;display:block;background:#111;}
  #loading-cover{
    position:absolute;top:0;left:0;width:100%;height:100%;
    display:flex;flex-direction:column;align-items:center;justify-content:center;
    background:#111;
  }
  #unity-logo img{max-width:280px;margin-bottom:20px;pointer-events:none;}
  #unity-progress-bar-empty{
    width:40%;height:8px;background:rgba(255,255,255,.15);
    border-radius:4px;overflow:hidden;
  }
  #unity-progress-bar-full{height:100%;width:0%;background:linear-gradient(90deg,#f97316,#fb923c);transition:width .3s ease;}
  .spinner,.spinner:after{border-radius:50%;width:4em;height:4em;}
  .spinner{
    margin:16px;font-size:10px;position:relative;text-indent:-9999em;
    border-top:0.8em solid rgba(255,255,255,.15);
    border-right:0.8em solid rgba(255,255,255,.15);
    border-bottom:0.8em solid rgba(255,255,255,.15);
    border-left:0.8em solid #fb923c;
    animation:spin 1.1s infinite linear;
  }
  @keyframes spin{0%{transform:rotate(0deg)}100%{transform:rotate(360deg)}}
  .load-label{color:#aaa;font-family:Arial,sans-serif;font-size:13px;margin-top:10px;}
</style>
</head>
<body class="dark">

<div id="unity-container" class="unity-desktop">
  <canvas id="unity-canvas" tabindex="-1"></canvas>
</div>
<div id="loading-cover">
  <div id="unity-logo"><img src="https://cdn.jsdelivr.net/gh/ftbaldana/yojex@main/Images/logo.png" onerror="this.style.display='none'"/></div>
  <div class="spinner" id="spinner"></div>
  <div id="unity-progress-bar-empty" style="display:none;">
    <div id="unity-progress-bar-full"></div>
  </div>
  <div class="load-label" id="load-label">Loading... g+</div>
</div>

<script>
/* ============================================================
   YANDEX SDK STUB — sdk.js YÜKLENMEDEN ÖNCE
   lang:"en" — Rusça açılma sorunu çözülür
   ============================================================ */
window.YaGames = {
  init: function() {
    var sdk = {
      environment: {
        app:     { id: '0' },
        i18n:    { lang: 'en', tld: 'com' },
        browser: { lang: 'en' },
        payload: null
      },
      deviceInfo: {
        type: 'desktop',
        isMobile:  function(){ return false; },
        isDesktop: function(){ return true;  },
        isTablet:  function(){ return false; },
        isTV:      function(){ return false; }
      },
      adv: {
        showFullscreenAdv: function(o){
          if(o&&o.callbacks&&o.callbacks.onClose) o.callbacks.onClose(false);
        },
        showRewardedVideo: function(o){
          if(!o||!o.callbacks) return;
          if(o.callbacks.onOpen)     o.callbacks.onOpen();
          if(o.callbacks.onRewarded) o.callbacks.onRewarded();
          if(o.callbacks.onClose)    o.callbacks.onClose();
        },
        showBannerAdv:      function(){},
        hideBannerAdv:      function(){},
        getBannerAdvStatus: function(){ return Promise.resolve({ stickyAdvIsShowing: false }); }
      },
      auth:     { openAuthDialog: function(){ return Promise.resolve(); } },
      feedback: {
        canReview:     function(){ return Promise.resolve({ value: false, reason: '' }); },
        requestReview: function(){ return Promise.resolve({ feedbackSent: false }); }
      },
      shortcut: {
        canShowPrompt: function(){ return Promise.resolve({ canShow: false }); },
        showPrompt:    function(){ return Promise.resolve({ outcome: 'rejected' }); }
      },
      getPayments: function(){ return Promise.reject('no payments'); },
      getPlayer: function() {
        return Promise.resolve({
          isAuthorized:    function(){ return false; },
          getName:         function(){ return ''; },
          getUniqueID:     function(){ return '0'; },
          getPhoto:        function(){ return ''; },
          getPayingStatus: function(){ return 'unknown'; },
          setData:         function(){ return Promise.resolve(); },
          getData:         function(){ return Promise.resolve({}); },
          getStats:        function(){ return Promise.resolve({}); },
          setStats:        function(){ return Promise.resolve(); }
        });
      },
      getFlags:   function(){ return Promise.resolve({}); },
      serverTime: function(){ return Promise.resolve(Date.now()); },
      on:         function(){},
      features: {
        LoadingAPI:  { ready: function(){} },
        GameplayAPI: { start: function(){}, stop: function(){} }
      }
    };
    return Promise.resolve(sdk);
  }
};
console.log('[Stub] YaGames hazir, lang="en"');
</script>

<script>
(function(){
  var s = document.createElement('script');
  s.src = '/sdk.js';
  s.onerror = function(){ console.warn('[Stub] sdk.js yuklenemedi'); };
  document.head.appendChild(s);
})();
</script>

<script>
/* ============================================================
   CONFIG — CDN adresini doldurun
   ============================================================ */
var CDN        = "https://cdn.jsdelivr.net/gh/ftbaldana/yojex@main/Build";   /* <-- degistir */
var DATA_PARTS = 4;   /* a4e4e458...data.br  28MB  / 8MB = 4 part */
var WASM_PARTS = 5;   /* 7596a494...wasm.br  37.7MB/ 8MB = 5 part */

var LOADER    = "ca98492c4e603014560541ecf62d8174";
var DATA_FILE = "a4e4e4588738c16ec10d71d22926ae68";
var FRAMEWORK = "a25d3e539f772ceb6e8712015eceb9e4";
var WASM_FILE = "7596a49439dcfec08e5215bed2a9eec3";

var buildUrl = CDN;

var config = {
  dataUrl:            null,
  frameworkUrl:       buildUrl + "/" + FRAMEWORK + ".framework.js",
  codeUrl:            null,
  streamingAssetsUrl: "StreamingAssets",
  companyName: "polar men",
  productName: "Dig out of Prison",
  productVersion: "0.1"
};

var container        = document.querySelector("#unity-container");
var canvas           = document.querySelector("#unity-canvas");
var loadingCover     = document.querySelector("#loading-cover");
var progressBarEmpty = document.querySelector("#unity-progress-bar-empty");
var progressBarFull  = document.querySelector("#unity-progress-bar-full");
var spinner          = document.querySelector("#spinner");
var loadLabel        = document.querySelector("#load-label");

if (/iPhone|iPad|iPod|Android/i.test(navigator.userAgent))
  container.className = "unity-mobile";

var NO_DATA = 'no data';
var StartUnityInstance;
var ygGameInstance = null;
var ysdk = null;
var initYSDK = false;
var initGame = false;
var syncInit = false;
var isVisibility = true;
var isPausedGame = false;
var cloudSaves = NO_DATA;
var statsSaves = NO_DATA;
var reviewData = 'false';
var gameLabelData = 'false';
var paymentsData = NO_DATA;
var payments = null;
var player = null;
var environmentData = NO_DATA;
var playerData = NO_DATA;
var flasgsData = NO_DATA;
var nowFullAdOpen = false;

document.addEventListener('contextmenu', function(e){ e.preventDefault(); });
document.addEventListener('visibilitychange', function(){ SetVisibility(!document.hidden); });
window.addEventListener('blur',  function(){ SetVisibility(false); });
window.addEventListener('focus', function(){ SetVisibility(true); });
window.addEventListener('pointerdown', function(){ FocusGame(); SetVisibility(true); });

/* requestPointerLock patch */
var oRPL = Element.prototype.requestPointerLock;
Element.prototype.requestPointerLock = async function hkRPL() {
  var args = Array.from(arguments);
  try {
    (args[0] = args[0] || {}).unadjustedMovement = true;
    await oRPL.apply(this, args);
  } catch(e){ console.log(e); }
};

function InstallBlurFocusBlocker() {
  if (!('onblur' in document) || window.blurFocusHandlersInstalled) return;
  window.addEventListener('blur', function() {
    var btn = document.createElement('button');
    Object.assign(btn.style, {position:'fixed',top:'0',left:'0',width:'100%',height:'100%',
      zIndex:'9999',backgroundColor:'rgba(0,0,0,0)',border:'none',cursor:'default'});
    document.body.appendChild(btn);
    window.addEventListener('focus', function rem(){
      btn.parentNode && btn.parentNode.removeChild(btn);
      window.removeEventListener('focus', rem);
    });
  });
  window.blurFocusHandlersInstalled = true;
}

function FocusGame(){ if (!isPausedGame) canvas.focus(); }

function SetVisibility(v) {
  if (ysdk !== null && initGame === true && isVisibility !== v) {
    isVisibility = v;
    YG2Instance('SetFocusWindowGame', v ? 'true' : 'false');
  }
}

function PauseCallback()  { isPausedGame = true;  YG2Instance('SetPauseGame', 'true');  }
function ResumeCallback() { isPausedGame = false; YG2Instance('SetPauseGame', 'false'); }

/* ── PART İNDİRME ─────────────────────────────────────────── */
async function fetchParts(baseUrl, partCount, label) {
  var parts = [];
  for (var i = 0; i < partCount; i++) {
    loadLabel.textContent = label + ' ' + (i+1) + '/' + partCount + '...';
    var r = await fetch(baseUrl + '.part' + i);
    if (!r.ok) throw new Error('part' + i + ': HTTP ' + r.status);
    parts.push(new Uint8Array(await r.arrayBuffer()));
  }
  var total  = parts.reduce(function(s,p){ return s+p.length; }, 0);
  var merged = new Uint8Array(total);
  var off = 0;
  for (var j = 0; j < parts.length; j++){ merged.set(parts[j], off); off += parts[j].length; }
  return merged;
}

function brotliBlobUrl(uint8, filename) {
  var blob = new Blob([uint8], { type:'application/octet-stream' });
  return URL.createObjectURL(blob) + '#' + encodeURIComponent(filename);
}

/* ── LOADER ───────────────────────────────────────────────── */
var script = document.createElement("script");
script.src = buildUrl + "/" + LOADER + ".loader.js";
script.onload = async function() {
  try {
    var dataBytes = await fetchParts(buildUrl + "/" + DATA_FILE + ".data.br", DATA_PARTS, 'Data');
    var wasmBytes = await fetchParts(buildUrl + "/" + WASM_FILE + ".wasm.br", WASM_PARTS, 'WASM');
    config.dataUrl = brotliBlobUrl(dataBytes, DATA_FILE + ".data.br");
    config.codeUrl = brotliBlobUrl(wasmBytes,  WASM_FILE + ".wasm.br");
  } catch(e) {
    console.error("Download error:", e);
    loadLabel.textContent = 'Error: ' + e.message;
    return;
  }

  StartUnityInstance = function() {
    createUnityInstance(canvas, config, function(progress){
      spinner.style.display = "none";
      progressBarEmpty.style.display = "";
      progressBarFull.style.width = (100 * Math.max(progress, 0.05)) + '%';
    }).then(function(inst){
      ygGameInstance = inst;
      loadingCover.style.display = "none";
    }).catch(function(msg){ console.error(msg); });
  };

  InstallBlurFocusBlocker();
  /* Her iki yol için: SDK bitmişse hemen başlat */
  StartUnityInstance_IfUnloaded();
};

function StartUnityInstance_IfUnloaded(){
  if (typeof StartUnityInstance === 'function' && spinner.style.display !== "none")
    StartUnityInstance();
}
function IsLocalHost(){
  try {
    if (window.top !== window) return false;
    var h = window.location.hostname;
    return h==="localhost"||h==="127.0.0.1"||h.endsWith(".local");
  } catch(e){ return false; }
}

function YG2Instance(method, arg) {
  if (ygGameInstance == null) return;
  var send = function(){
    arg !== undefined
      ? ygGameInstance.SendMessage('YG2Instance', method, arg)
      : ygGameInstance.SendMessage('YG2Instance', method);
  };
  if (!initGame) setTimeout(send, 100); else send();
}
function LogStyledMessage(msg){
  console.log('%c'+msg,'color:#FFDF73;background-color:#454545');
}

/* ── SDK INIT ─────────────────────────────────────────────── */
InitYSDK();
async function InitYSDK() {
  try {
    if (IsLocalHost()) return;
    ysdk = await YaGames.init();
    ysdk.on('game_api_pause',  PauseCallback);
    ysdk.on('game_api_resume', ResumeCallback);
    await RequestingEnvironmentData(); LogStyledMessage('Init EnvirData ysdk');
    await InitPlayer();               LogStyledMessage('Init Authorization ysdk');
    await LoadCloud();                LogStyledMessage('Init Storage ysdk');
    await InitReview();               LogStyledMessage('Init Review ysdk');
    await GetStats();                 LogStyledMessage('Init PlayerStats ysdk');
    await InitPayments();             LogStyledMessage('Init Payments ysdk');
    await InitGameLabel();            LogStyledMessage('Init GameLabel ysdk');
    await GetFlags();                 LogStyledMessage('Init Flags ysdk');
    initYSDK = true;
    if (ygGameInstance != null) ygGameInstance.SendMessage('YG2Instance','InitSDKComplete');
    LogStyledMessage('Init YandexSDK Success');
  } catch(e){ console.error('CRASH Init SDK:', e); }
  if (!IsLocalHost() && !syncInit) StartUnityInstance_IfUnloaded();
}

function InitGame(){
  initGame = true;
  setTimeout(function(){ if(isPausedGame) YG2Instance('SetPauseGame','true'); }, 100);
}

LogStyledMessage('[PluginYG2 v2.007] [Build: 145] [Platform: YandexGames]');

/* ── CLOUD SAVE ───────────────────────────────────────────── */
function SaveCloud(jsonData, flush) {
  if (!player){ console.error('CRASH Save Cloud: no player'); return; }
  try { player.setData({ saves:[jsonData] }, flush); } catch(e){ console.error(e); }
}
function LoadCloud() {
  return new Promise(function(resolve){
    if (!ysdk){ Final(NO_DATA); return; }
    try {
      player.getData(["saves"]).then(function(data){
        Final(data.saves ? JSON.stringify(data.saves) : NO_DATA);
      }).catch(function(){ console.error('Load Cloud Error!'); Final(NO_DATA); });
    } catch(e){ console.error(e); Final(NO_DATA); }
    function Final(r){ cloudSaves=r; YG2Instance('SetLoadSaves',r); resolve(r); }
  });
}

/* ── STATS ────────────────────────────────────────────────── */
function GetStats() {
  return new Promise(function(resolve){
    if (!ysdk){ Final(NO_DATA); return; }
    try {
      player.getStats().then(function(s){ Final(JSON.stringify(s)); })
        .catch(function(e){ console.error(e); Final(NO_DATA); });
    } catch(e){ console.error(e); Final(NO_DATA); }
    function Final(r){ statsSaves=r; YG2Instance('ReceiveStats',r); resolve(r); }
  });
}
function SetStats(jsonStats) {
  if (!ysdk||!player){ console.error('SetStats: not init'); return; }
  try { player.setStats(JSON.parse(jsonStats)); } catch(e){ console.error(e); }
}

/* ── ADS ──────────────────────────────────────────────────── */
function StickyAdActivity(show) {
  try {
    ysdk.adv.getBannerAdvStatus().then(function(d){
      if (d.stickyAdvIsShowing){ if (!show) ysdk.adv.hideBannerAdv(); }
      else if (show) ysdk.adv.showBannerAdv();
    });
  } catch(e){ console.error(e); }
}
function RewardedAdvShow(id) {
  try {
    ysdk.adv.showRewardedVideo({ callbacks: {
      onOpen:     function(){ LogStyledMessage('Opened Rewarded Adv'); YG2Instance('OpenRewardedAdv'); },
      onClose:    function(){ LogStyledMessage('Closed Rewarded Adv'); YG2Instance('CloseRewardedAdv'); FocusGame(); },
      onRewarded: function(){ YG2Instance('RewardAdv', id); },
      onError:    function(err){ console.error(err); YG2Instance('ErrorRewardedAdv'); }
    }});
  } catch(err){ console.error('CRASH Rewarded:', err.message); }
}
var nowFullAdOpen = false;
function InterAdvShow() {
  try {
    if (!ysdk||nowFullAdOpen) return;
    ysdk.adv.showFullscreenAdv({ callbacks: {
      onOpen:  function(){ nowFullAdOpen=true; if(initGame) YG2Instance('OpenInterAdv'); },
      onClose: function(was){ nowFullAdOpen=false; if(initGame) YG2Instance('CloseInterAdv', was?'true':'false'); FocusGame(); },
      onError: function(e){ console.error(e); nowFullAdOpen=false; YG2Instance('ErrorInterAdv'); FocusGame(); }
    }});
  } catch(e){ console.error(e); }
}

/* ── REVIEW ───────────────────────────────────────────────── */
function InitReview() {
  return new Promise(function(resolve){
    if (!ysdk){ resolve('false'); return; }
    try {
      ysdk.feedback.canReview().then(function(d){
        if (d.value) reviewData='true';
        resolve(d.value?'true':'false');
      });
    } catch(e){ resolve('false'); }
  });
}
function Review() {
  try {
    ysdk.feedback.canReview().then(function(d){
      if (d.value) {
        ysdk.feedback.requestReview().then(function(r){
          YG2Instance('ReviewSent', r.feedbackSent?'true':'false');
          FocusGame();
        });
      } else FocusGame();
    });
  } catch(e){ console.error(e); FocusGame(); }
}

/* ── PAYMENTS ─────────────────────────────────────────────── */
function InitPayments() {
  return new Promise(function(resolve){
    try {
      if (!ysdk){ Final(NO_DATA); return; }
      ysdk.getPayments().then(function(_p){
        payments=_p;
        payments.getCatalog().then(function(products){
          payments.getPurchases().then(function(purchases){
            var ids=[],titles=[],descs=[],imgs=[],prices=[],priceVals=[],codes=[],imgURLs=[],cons=[];
            for(var i=0;i<products.length;i++){
              ids[i]=products[i].id; titles[i]=products[i].title;
              descs[i]=products[i].description; imgs[i]=products[i].imageURI;
              prices[i]=products[i].price; priceVals[i]=products[i].priceValue;
              codes[i]=products[i].priceCurrencyCode;
              imgURLs[i]=products[i].getPriceCurrencyImage("medium");
              cons[i]=!purchases.some(function(p){ return p.productID===ids[i]; });
            }
            Final(JSON.stringify({id:ids,title:titles,description:descs,imageURI:imgs,
              price:prices,priceValue:priceVals,priceCurrencyCode:codes,
              currencyImageURL:imgURLs,consumed:cons,
              language:ysdk.environment.i18n.lang}));
          });
        });
      }).catch(function(){ LogStyledMessage('Purchases not available'); Final(NO_DATA); });
    } catch(e){ console.error(e); Final(NO_DATA); }
    function Final(r){ paymentsData=r; YG2Instance('PaymentsEntries',r); resolve(r); }
  });
}
function BuyPayments(id) {
  try {
    if (!payments){ YG2Instance('OnPurchaseFailed',id); return; }
    payments.purchase(id).then(function(){ ConsumePurchase(id,true); FocusGame(); })
      .catch(function(e){ console.error(e); YG2Instance('OnPurchaseFailed',id); FocusGame(); });
  } catch(e){ console.error(e); YG2Instance('OnPurchaseFailed',id); FocusGame(); }
}
function ConsumePurchase(id, ok) {
  try {
    if (!payments) return;
    payments.getPurchases().then(function(ps){
      for(var i=0;i<ps.length;i++){
        if(ps[i].productID===id){
          payments.consumePurchase(ps[i].purchaseToken);
          if(ok) YG2Instance('OnPurchaseSuccess',id);
        }
      }
    });
  } catch(e){ console.error(e); }
}
function ConsumePurchases(ok) {
  try {
    if (!payments) return;
    payments.getPurchases().then(function(ps){
      for(var i=0;i<ps.length;i++){
        payments.consumePurchase(ps[i].purchaseToken);
        if(ok) YG2Instance('OnPurchaseSuccess',ps[i].productID);
      }
    });
  } catch(e){ console.error(e); }
}

/* ── GAME LABEL ───────────────────────────────────────────── */
function InitGameLabel() {
  return new Promise(function(resolve){
    if (!ysdk){ resolve('false'); return; }
    try {
      ysdk.shortcut.canShowPrompt().then(function(p){
        if(p.canShow){ gameLabelData='true'; resolve('true'); }
        else resolve('false');
      });
    } catch(e){ console.error(e); resolve('false'); }
  });
}
function GameLabelShowDialog() {
  try {
    ysdk.shortcut.showPrompt().then(function(r){
      YG2Instance(r.outcome==='accepted'?'OnGameLabelSuccess':'OnGameLabelFail');
      FocusGame();
    });
  } catch(e){ console.error(e); FocusGame(); }
}

/* ── FLAGS ────────────────────────────────────────────────── */
function GetFlags() {
  return new Promise(function(resolve){
    if (!ysdk){ Final(NO_DATA); return; }
    try {
      ysdk.getFlags().then(function(flags){
        var names=[],values=[];
        for(var k in flags){ if(flags.hasOwnProperty(k)){ names.push(k); values.push(flags[k]); } }
        Final(JSON.stringify({names:names,values:values}));
      });
    } catch(e){ console.error(e); Final(NO_DATA); }
    function Final(r){ flasgsData=r; resolve(r); }
  });
}

/* ── ENVIRONMENT ──────────────────────────────────────────── */
function RequestingEnvironmentData() {
  return new Promise(function(resolve){
    if (!ysdk){ Final(NO_DATA); return; }
    try {
      var ua=navigator.userAgent, browser;
      if(ua.includes('YaBrowser')||ua.includes('YaSearchBrowser')) browser='Yandex';
      else if(ua.includes('Opera')||ua.includes('OPR')) browser='Opera';
      else if(ua.includes('Firefox')) browser='Firefox';
      else if(ua.includes('MSIE')) browser='IE';
      else if(ua.includes('Edge')) browser='Edge';
      else if(ua.includes('Chrome')) browser='Chrome';
      else if(ua.includes('Safari')) browser='Safari';
      else browser='Other';
      Final(JSON.stringify({
        language:   ysdk.environment.i18n.lang,
        domain:     ysdk.environment.i18n.tld,
        deviceType: ysdk.deviceInfo.type,
        isMobile:   ysdk.deviceInfo.isMobile(),
        isDesktop:  ysdk.deviceInfo.isDesktop(),
        isTablet:   ysdk.deviceInfo.isTablet(),
        isTV:       ysdk.deviceInfo.isTV(),
        appID:      ysdk.environment.app.id,
        browserLang:ysdk.environment.browser.lang,
        payload:    ysdk.environment.payload,
        platform:   navigator.platform,
        browser:    browser
      }));
    } catch(e){ console.error(e); Final(NO_DATA); }
    function Final(r){ environmentData=r; YG2Instance('SetEnvirData',r); resolve(r); }
  });
}

/* ── PLAYER ───────────────────────────────────────────────── */
async function InitPlayer() {
  return new Promise(async function(resolve){
    try {
      if (!ysdk) return Final(NotAuthorized(false));
      player = await ysdk.getPlayer();
      if (!player.isAuthorized()) return Final(NotAuthorized());
      return Final(JSON.stringify({
        playerAuth:'resolved', playerName:player.getName(),
        playerId:player.getUniqueID(), playerPhoto:player.getPhoto('medium'),
        payingStatus:player.getPayingStatus()
      }));
    } catch(e){ console.error(e); return Final(NotAuthorized(false)); }
    function Final(r){ playerData=r; YG2Instance('SetAuth',r); resolve(r); }
  });
}
function NotAuthorized(isInitSDK) {
  if (isInitSDK===undefined) isInitSDK=true;
  return JSON.stringify({
    playerAuth:'rejected', playerName:'unauthorized',
    playerId: isInitSDK&&player ? player.getUniqueID() : 'unauthorized',
    playerPhoto:'no data', payingStatus:'unknown'
  });
}
function OpenAuthDialog() {
  if (!ysdk) return;
  try {
    ysdk.auth.openAuthDialog().then(function(){
      InitPlayer().then(function(){ YG2Instance('LoggedIn'); });
    });
  } catch(e){ console.error(e); }
}

document.body.appendChild(script);
</script>

<script>
var isIOS=/iPad|iPhone|iPod/.test(navigator.userAgent)||
  (navigator.platform==='MacIntel'&&navigator.maxTouchPoints>1);
if(isIOS){
  document.documentElement.style.height='100%';
  document.body.style.cssText+=';height:100%;position:fixed;width:100%';
  document.addEventListener('touchmove',function(e){e.preventDefault();},{passive:false});
}
</script>
</body>
</html>


]]></Content>
</Module>
